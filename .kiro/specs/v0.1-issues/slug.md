the auto generating of slugs on editing pages does not work. it only sets one character before stoppingg. maybe the onblur isn't quite working. let' add button instead for user to indicate when ready to generate

i also see "Authorization token is required" error message sometimes
